# Create a token at https://globalfishingwatch.org/our-apis/
GFW_API_TOKEN=replace_me

# Alternatively, put this reference in .env and launch with op run --env-file=.env:
# GFW_API_TOKEN=op://Developer/Global Fishing Watch API Key/credential
# Never commit .env or a resolved token.
